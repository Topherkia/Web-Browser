
  # Web Browser with Ratings

  This is a code for Web Browser with Ratings.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run desktop` to start the development server.
  
